apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Reconciliation scope is intentionally infrastructure-only for now.
  # Enable platform after dependency ordering has been verified.
  - infrastructure
  - platform
